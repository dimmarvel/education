#include <iostream>
int main()
{
    f(v[i], i++);         // v[i] OR i++ was called first
    int some = 102491294; // 8/16/32 cant take this numb in int
    std::system("pause"); // not find on linux
    char c = 255;
    if (c == -1) // sygnal machine error
}
